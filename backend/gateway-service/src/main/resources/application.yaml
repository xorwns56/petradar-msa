spring:
  application:
    name: gateway-service
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health, info, gateway, prometheus  # prometheus 엔드포인트 노출
  endpoint:
    gateway:
      enabled: true
    prometheus:
      enabled: true  # /actuator/prometheus 활성화
