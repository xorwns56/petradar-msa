fastapi==0.115.0
uvicorn[standard]==0.30.6   # FastAPI 실행 서버 (Spring의 내장 Tomcat과 같은 역할)
psycopg2-binary==2.9.9      # PostgreSQL 드라이버 (Spring의 JDBC 드라이버)
sqlalchemy==2.0.36          # Python ORM (Spring의 JPA)
pgvector==0.3.2             # pgvector SQLAlchemy 연동
transformers==4.46.0        # HuggingFace 모델 라이브러리 (CLIP 포함)
torch==2.5.1                # CLIP 모델 실행에 필요한 딥러닝 프레임워크
Pillow==11.0.0              # 이미지 처리 라이브러리
python-dotenv==1.0.1        # .env 파일로 환경변수 관리
python-multipart==0.0.12    # Form 데이터 처리 (이미지 업로드)
prometheus-fastapi-instrumentator==7.0.0  # Prometheus 메트릭 수집
faststream[kafka]==0.5.30                  # 선언적 Kafka consumer (Spring @KafkaListener와 유사)
minio==7.2.9                              # MinIO 이미지 다운로드
pytest==8.3.3                             # 테스트 프레임워크
pytest-asyncio==0.24.0                    # 비동기 테스트 지원
httpx==0.27.2                             # FastAPI 테스트용 비동기 HTTP 클라이언트
